import { ButtonRowProps } from "./button-row.type";

export const useButtonOptions = (props: ButtonRowProps) => {
  const { titleIcon, bottom, rigthIcons, badge, inside, outside } = props;

  return {};
};
